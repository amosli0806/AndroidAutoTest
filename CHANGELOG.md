# 更新日志

> 每次发版前，在顶部新增一个 `## vX.Y.Z` 段落写清这一版的更新说明。
> CI 发版时会自动把这个段落填进 Release 正文（应用内「检查更新」弹窗展示的就是它），
> 无需再手动去网页编辑。格式约定：段落标题必须是 `## vX.Y.Z`（与 utils/version.py 一致）。
>
> 日常改代码时，把改动**实时记到下方「未发布」段落**；决定发版时把标题 `## 未发布`
> 改成 `## vX.Y.Z`（版本号），即可作为该版更新说明。

## 未发布（下一个版本）

- 新增：元素操作步骤（点击/双击/长按/输入）执行前自动等待元素出现，默认 10 秒，避免页面还没渲染完就点
- 新增：上述四类步骤的卡片与编辑对话框都加了「超时秒数」可配项（默认 10，可单独调整；0 表示不等待）
- 优化：录制生成的步骤回放时同样享受元素等待机制（语义定位自动等，坐标定位按分辨率自适应）
- 优化：录制反查时机后移 —— 抬手后等页面稳定再抓取控件，避免点击动画/路由切换导致控件对应不上
- 优化：录制时资源ID重复的元素（列表项）记录实例序号，回放精确定位到第 N 个，不再点错行
- 优化：录制反查同时保留资源ID与文本，回放时一个失效自动退回另一个（双保险）
- 修复：录制停止后生成步骤耗时翻倍的问题 —— 输入框识别不再重复 dump（复用反查缓存，普通点击零额外开销）
- 优化：录制停止后增加「正在生成步骤」Toast 提示，消除无反馈等待
- 优化：连续快速点击时自动跳过反查稳定等待，整体生成速度更快
- 修复：更新步骤弹窗中「instance」等英文技术字段改为中文显示（实例序号/备用定位方式/备用定位值），并附用途说明
- 修复：更新步骤弹窗的「从元素库选择」按钮图标未适配夜间模式的问题（现跟随弹窗主题）
- 优化：录制完成确认对话框重新设计（步骤卡片式列表、夜间模式适配、按钮样式与全局统一）
- 优化：确认对话框点击行内任意位置即可勾选/取消（不再需要精确点到复选框），勾选状态用绿色勾图标+行高亮表达，副标题实时显示已选数量
- 优化：选择元素对话框的确定/取消按钮调整为统一尺寸
- 新增：录制支持软键盘输入识别 —— 点击输入框并打字会自动生成「输入」步骤（含文字内容）
- 新增：录制完成后弹预览确认框，可逐条勾选要保留的步骤（默认全选，支持全选/全不选），避免误录步骤直接写入

## v1.1.5

- 新增：执行页支持中途停止（执行按钮点击后就地变「停止」）
- 修复：执行清单点复选框偶尔勾不上的问题
- 优化：ADB 工具箱执行期间放行 logcat / 录屏 / 截图 / 导出日志等只读命令，只拦会改设备状态的命令，被拦下不再卡「启动中」
- 优化：语音播报右栏默认不勾选，部分选中方块与外框同心
- 修复：报告标题去掉多写的「驭」字
- 工具：新增发版脚本 release.py（一条命令自动改版本号/写更新说明/提交/推送/打 tag）

## v1.1.4

- 修复：主菜单图标（设置 / 检查更新 / 关于）在夜间模式下看不清的问题，现在跟随主题切换颜色
- 修复：更新包下载加速 —— 自动更新下载走 gh-proxy.com 镜像，国内网络也能正常更新（检查与校验仍直连官方，安全不受影响）
- 修复：更新包资产选择逻辑，避免误选到历史残缺的旧包
- 优化：完善发版自动化（更新说明随代码管理、自测桩版本动态化）

## v1.1.3

- 首个通过 CI 自动发布的版本
- 新增：自动更新能力（启动检查 / 手动检查 / 下载更新 / 独立更新器替换文件）
- 新增：语音播报、消息中心、ADB 工具箱重构为内嵌面板
- 版本号统一由 utils/version.py 单一来源管理
