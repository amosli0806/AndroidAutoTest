# 更新日志

> 每次发版前，在顶部新增一个 `## vX.Y.Z` 段落写清这一版的更新说明。
> CI 发版时会自动把这个段落填进 Release 正文（应用内「检查更新」弹窗展示的就是它），
> 无需再手动去网页编辑。格式约定：段落标题必须是 `## vX.Y.Z`（与 utils/version.py 一致）。
>
> 日常改代码时，把改动**实时记到下方「未发布」段落**；决定发版时把标题 `## 未发布`
> 改成 `## vX.Y.Z`（版本号），即可作为该版更新说明。

## v1.1.8

### 问题修复

- **修复升级后仍残留 `_internal.old` 文件夹删不掉的问题** —— 升级时自动关闭旧版可视化服务进程，残留备份可彻底清理，无需手动结束进程

## v1.1.7

### 问题修复

- **修复安装版「应用可视化」偶发启动崩溃的问题** —— 内置可视化服务程序在部分环境会因依赖缺失报错退出，现已彻底修复
- **修复升级后残留 `_internal.old` 文件夹删不掉的问题** —— 升级时自动关闭旧版后台进程，旧版备份可正常清理，无需再手动结束进程

## v1.1.6

### 新增

- 步骤列表支持批量删除：Ctrl+点击多选（Shift 连选、Ctrl+A 全选），按 Delete 键或右键菜单一次删除
- 元素操作步骤（点击/双击/长按/输入）执行前自动等待元素出现，避免页面没加载完就操作；超时秒数可调
- 录制支持软键盘输入：点击输入框并打字，自动生成「输入」步骤
- 录制完成后弹窗确认：逐条勾选要保留的步骤，避免误录直接写入
- 录制时同一元素重复点击自动合并，列表类元素精确记录点到第几个
- 新增帮助中心「环境准备」指引：未装 adb 的电脑照着几步装好

### 问题修复

- **修复安装版（exe）打开「应用可视化」报「启动超时，端口未开放」的问题** —— 安装版现在内置独立的可视化服务程序，随包分发，开箱即用
- 彻底解决插拔设备时反复闪出黑色/白色小窗口的问题
- 设备插拔时消息面板不再刷「连接中断/已恢复」，只有真的断线才会提示
- 打开消息中心页时插拔设备不再闪窗
- 提示气泡偶发变成白色方块的问题
- 关闭虫师后后台残留进程不退出的问题
- 「更新步骤」弹窗的英文技术字段改为中文，并附说明

### 体验优化

- 录制完成确认界面全新设计：每个步骤一张卡片、间距清晰，点击行即可勾选，实时显示已选数量
- 「更新步骤」弹窗滚动条改为细条样式，与其他页面一致
- 录制更聪明：抬手后等页面稳定再识别控件、重复元素自动记录序号、定位失效自动换备用方式，整体生成更快
- 使用电脑已安装的 adb（不再内置），避免版本冲突；投屏功能不受影响

## v1.1.5

- 新增：执行页支持中途停止（执行按钮点击后就地变「停止」）
- 修复：执行清单点复选框偶尔勾不上的问题
- 优化：ADB 工具箱执行期间放行 logcat / 录屏 / 截图 / 导出日志等只读命令，只拦会改设备状态的命令，被拦下不再卡「启动中」
- 优化：语音播报右栏默认不勾选，部分选中方块与外框同心
- 修复：报告标题去掉多写的「驭」字
- 工具：新增发版脚本 release.py（一条命令自动改版本号/写更新说明/提交/推送/打 tag）

## v1.1.4

- 修复：主菜单图标（设置 / 检查更新 / 关于）在夜间模式下看不清的问题，现在跟随主题切换颜色
- 修复：更新包下载加速 —— 自动更新下载走 gh-proxy.com 镜像，国内网络也能正常更新（检查与校验仍直连官方，安全不受影响）
- 修复：更新包资产选择逻辑，避免误选到历史残缺的旧包
- 优化：完善发版自动化（更新说明随代码管理、自测桩版本动态化）

## v1.1.3

- 首个通过 CI 自动发布的版本
- 新增：自动更新能力（启动检查 / 手动检查 / 下载更新 / 独立更新器替换文件）
- 新增：语音播报、消息中心、ADB 工具箱重构为内嵌面板
- 版本号统一由 utils/version.py 单一来源管理
