# utils/version.py
"""应用版本号的**唯一来源**。

为什么要有这个文件：版本号原先散在三处 —— 「关于」对话框标题、复制到剪贴板的文本、
以及启动封面（那张 PNG 是画出图里的）—— 结果三处各说各话
（对话框显示 V1.0.1、剪贴板和封面是 V1.1.3）。

现在只改这里：
  * 「关于 虫师」对话框（views/main_window.py）直接 import 这两个常量；
  * 启动封面在**打包时**由 main.spec 把 APP_VERSION 画到底图上，
    所以封面上的版本号会自动跟着这里走，不用再去改图片。
改完重新打包即可，没有第二处要同步。
"""

APP_VERSION = "1.1.3"

# 构建时间（「关于」对话框里跟版本号一起显示）
BUILD_DATE = "2026-09-21 12:26:35"
